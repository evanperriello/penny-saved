var React = require("react");
//Arrow function instead of a nameless function
var PrintButton = (props) => {
    return (
        <div>
            <h1>PrintButton Component</h1>
        </div>
    );
};

module.exports = PrintButton;