var React = require("react");
//Arrow function instead of a nameless function
var Form = (props) => {
    return (
        <div>
            <h1>Form Component</h1>
        </div>
    );
};

module.exports = Form;